import * as types from "./cart.actiontype"
const initialState={
    loading:false,
    error:false,
    Cart:[]    
}
export const Cartreducer=(state=initialState,{type,payload})=>{

    switch (type){
       
        default :{
            return state
        }
    }

}